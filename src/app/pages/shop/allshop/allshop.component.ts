import { Component } from '@angular/core';

/**
 * Componente que representa la página con todos los productos de la tienda.
 * 
 * Este componente se encarga de mostrar una lista de todos los productos disponibles
 * en la tienda. Se espera que se complemente con lógica para manejar la visualización
 * de productos, categorías, filtros, y otras funcionalidades relacionadas con el catálogo
 * de productos.
 */
@Component({
  selector: 'app-allshop',  // Selector que identifica al componente en el HTML
  templateUrl: './allshop.component.html',  // Archivo de plantilla asociado al componente
  styleUrls: ['./allshop.component.css']  // Archivo de estilos asociados al componente
})
export class AllshopComponent {
  // Aquí se pueden agregar propiedades y métodos relacionados con la lógica del componente
}
