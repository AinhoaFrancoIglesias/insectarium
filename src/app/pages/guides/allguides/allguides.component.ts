import { Component } from '@angular/core';

/**
 * Componente que agrupa todas las guías de la aplicación.
 * 
 * Este componente se utiliza como contenedor para mostrar diversas guías relacionadas, permitiendo a los usuarios acceder a todas ellas desde una única vista.
 * 
 * @component
 */
@Component({
  selector: 'app-allguides',  // Selector para utilizar el componente en HTML
  templateUrl: './allguides.component.html',  // Archivo HTML asociado al componente
  styleUrl: './allguides.component.css'  // Archivo de estilos CSS asociado al componente
})
export class AllguidesComponent {

}
